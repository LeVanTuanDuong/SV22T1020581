@model (string DisplayName, string Photo)
@{
    var photoSrc = string.IsNullOrWhiteSpace(Model.Photo)
        ? Url.Content("~/lib/adminlte/assets/img/user2-160x160.jpg")
        : Url.Content($"~/images/employees/{Model.Photo}");
}
<li class="nav-item dropdown user-menu">
    <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
        <img src="@photoSrc" class="user-image rounded-circle shadow" alt="Avatar" />
        <span class="d-none d-md-inline">@Model.DisplayName</span>
    </a>
    <ul class="dropdown-menu dropdown-menu-lg dropdown-menu-end">
        <li class="user-header text-bg-primary">
            <img src="@photoSrc" class="rounded-circle shadow" alt="Avatar" />
            <p>@Model.DisplayName</p>
        </li>
        <li class="user-footer">
            <a href="~/Account/ChangePassword" class="btn btn-default btn-flat">Đổi mật khẩu</a>
            <a href="~/Account/Logout" class="btn btn-default btn-flat float-end">Thoát</a>
        </li>
    </ul>
</li>
